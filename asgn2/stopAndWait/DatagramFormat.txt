Stop and Wait - Datagram format

the datagram is a 126-byte packet

the first byte contains the sender sequence number

the second byte contains
  * the number of data bytes in the packet
  * 125 if the last packet was sent

the remaining 124 bytes contain binary data from the file (at most 124 bytes)
